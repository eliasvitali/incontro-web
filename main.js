let root = document.documentElement;

document.addEventListener("DOMContentLoaded", _ => {
    

    
    //get data
    // $.getJSON('../data/x.json', function(result) { 
           
    // });
    
    
});

//all click events
document.addEventListener("click", e => {
    
    //if matches
    
});
